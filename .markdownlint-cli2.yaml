config:
  # Allow long lines — tables and code blocks exceed 80 chars
  MD013: false
  # Allow inline HTML — used in Mermaid diagrams
  MD033: false
  # Allow duplicate headings in different sections
  MD024:
    siblings_only: true
  # Allow compact table style (no space around pipes)
  MD060: false
